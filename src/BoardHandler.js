class BoardHandler {
    constructor() {}

    getPossibleMoves(piece, bState) {
        
    }

    isInCheck(kingPiece, bState) {
        let kx = kingPiece.x;
        let ky = kingPiece.y;
        
        // check x axis for white pieces that can attack the king
        for (let i = 0; i << 8; i++) {

        }
    }
}